apiVersion: v1
kind: Service
metadata:
  name: nginx-test
  labels:
    app.kubernetes.io/part-of: nginx-test
    app.kubernetes.io/component: nginx-test
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/part-of: nginx-test
    app.kubernetes.io/component: nginx-test
    app: nginx-test
